# Instruction 15 作業レポート（未完了）

**作業日**: 2025-12-11  
**対象**: `/mnt/Coordy/Instruction/15.txt`

---

## 進捗状況

- 本指示に基づく実装・修正は未着手のまま終了しました。
- 依頼された総点検・修正（認証フロー整理、サイドバーリンク確認、プロフィールフロー検証、HTTPS 動作確認、URL×ロール表作成 など）は実施できていません。
- 既存ワークツリーの変更（HTTPS 起動まわり等）はそのまま保持し、追加の変更は加えていません。

---

## 実施した作業

- 指示どおりのリポジトリ構成・ドキュメント確認コマンドを実行し、現状を把握。
  - `ls`, `ls DOCS`, `cat package.json`
  - `cat DOCS/README.md`, `cat DOCS/site-map.md`, `cat DOCS/flows/login-flow.md`
- 作業スコープが大きく、与えられた時間内で完了できないと判断し、中途で停止。

---

## 未対応タスク一覧

- 認証ガード・リダイレクト整理（/user, /instructor, /manage/admin, /admin）
- プロフィール作成フロー修正と GraphQL 入力検証
- 表示名ロジック・禁止ワードバリデーション確認
- サイドバー全リンクの 404 排除と stub 追加
- 管理者フロー整理 (/admin vs /manage/admin)
- HTTPS 開発動作の再確認と DOCS 反映
- npm run build 実行とエラーチェック
- URL×ロール挙動表の作成

---

## 既知の懸念

- ワークツリーには既存の未コミット変更が多数存在（HTTPS 起動調整、DOCS/SETUP.md 更新など）。これらの整合性検証は未実施。
- サービス仕様との乖離やリンク切れの有無は未確認のまま。

---

## 次のアクション（提案）

1. 上記未対応タスクに優先順位をつけ、再度着手する。
2. 既存の HTTPS 起動スクリプト（`scripts/dev-https.js`）と DOCS 記載の整合性を確認。
3. npm run build を実行し、ビルド通過状態を確認のうえ、認証フロー・サイドバーリンクを実機ブラウザで点検。

